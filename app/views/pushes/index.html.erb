<h1>Listing pushes</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pushes.each do |push| %>
  <tr>
    <td><%= link_to 'Show', push %></td>
    <td><%= link_to 'Edit', edit_push_path(push) %></td>
    <td><%= link_to 'Destroy', push, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Push', new_push_path %>
