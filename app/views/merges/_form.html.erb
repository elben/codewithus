<%= form_for(@merge) do |f| %>
  <% if @merge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@merge.errors.count, "error") %> prohibited this merge from being saved:</h2>

      <ul>
      <% @merge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
